apiVersion: v1
kind: Service
metadata:
  name: agent-registry
  labels:
    app.kubernetes.io/name: agent-registry
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-registry
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
